import { DsChartDirective } from './ds-chart.directive';

describe('DsChartDirective', () => {
  it('should create an instance', () => {
    const directive = new DsChartDirective();
    expect(directive).toBeTruthy();
  });
});
